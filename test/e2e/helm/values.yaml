controller:
  image:
    prefix: local
    name: syngit-controller
    tag: dev
  featureGates: ResourceFinder=true,HelmValuesMutation=true
providers:
  github:
    enabled: true
  gitlab:
    enabled: true
certmanager:
  dependency:
    enabled: false