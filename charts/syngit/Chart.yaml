apiVersion: v2
name: syngit
description: An operator to push resources to git
type: application
# version and appVersion are development placeholders and are not meant to be
# bumped by hand. The release workflow overrides both with the git tag when it
# packages the chart (helm package --version/--app-version).
version: v0.0.0-dev
appVersion: latest
home: https://github.com/syngit-org/syngit
icon: https://raw.githubusercontent.com/syngit-org/syngit/main/img/mini-icon.png
maintainers:
  - email: dassieu.damien@gmail.com
    name: Damien
